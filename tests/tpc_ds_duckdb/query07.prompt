

using the store sales data, return the name of items, and for each one the average quantity sold, average list price, average coupon amount, average sales price

for male customers who are single with College education in the year 2000 and did not come from the event or email promotion channels.

WHERE
    store_sales.customer_demographic.gender = 'M'
    and store_sales.customer_demographic.marital_status = 'S'
    and store_sales.customer_demographic.education_status = 'College'
    and (store_sales.promotion.channel_email = 'N' or store_sales.promotion.channel_event = 'N')
    and store_sales.date.year = 2000